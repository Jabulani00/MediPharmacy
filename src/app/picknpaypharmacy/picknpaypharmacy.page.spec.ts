import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PicknpaypharmacyPage } from './picknpaypharmacy.page';

describe('PicknpaypharmacyPage', () => {
  let component: PicknpaypharmacyPage;
  let fixture: ComponentFixture<PicknpaypharmacyPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(PicknpaypharmacyPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
