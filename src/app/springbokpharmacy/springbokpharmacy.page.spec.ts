import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SpringbokpharmacyPage } from './springbokpharmacy.page';

describe('SpringbokpharmacyPage', () => {
  let component: SpringbokpharmacyPage;
  let fixture: ComponentFixture<SpringbokpharmacyPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(SpringbokpharmacyPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
